# Changesets

This folder is managed by `@changesets/cli`.

- Run `bun run changeset` to add a release note.
- Run `bun run version-packages` to apply version + changelog updates.
- CI in `.github/workflows/release.yml` opens version PRs and publishes on merge.
